#### Exercise 2.5
The task was to create a function `any` which takes 2 strings *a* and *b* as arguments, and returns the index of the first character in *a* that is also in *b*, and returns -1 if there is no match.

I believe it's possible to implement the string-matching algorithm I used when
recreating the `grep` command in my `linux-commands` repository in order to make
the searching more efficient, however I decided to avoid doing that for now in
pursuit of gaining new knowledge on the C programming language.
