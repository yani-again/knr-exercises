#### Exercise 1.16
The task was to create a program which prints 'long' lines.

The threshold for 'long' wasn't defined, so I create a `THRESHOLD` variable
which I set to 30, meaning only lines over 30 characters long are printed.
